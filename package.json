{
  "name": "loopi",
  "version": "1.0.0",
  "description": "Slick turns a spec file into a short demo video.",
  "type": "module",
"bin": {
    "loopi": "./src/run.js"
  },
  "scripts": {
    "demo": "node src/run.js",
    "test": "vitest",
    "test:run": "vitest run",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  },
"devDependencies": {
    "@types/node": "^25.0.0",
    "prettier": "^3.0.0",
    "typescript": "^6.0.3",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
